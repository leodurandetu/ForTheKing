/* Leo
 * Retourne le maximum entre deux entiers */
int max(int a, int b) {
    return (a > b) ? a : b;
}

/* Leo
   Retourne le minimum entre deux entiers */
int min(int a, int b) {
    return (a < b) ? a : b;
}